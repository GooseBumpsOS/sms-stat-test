<!DOCTYPE HTML>
<html lang="ru">
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" type="text/css" href="{{ asset('assets/css/main.css') }}" />
    <title>Таблица SMS</title>
</head>

<table border="1">
    <tr>
        <th>Дата</th>
        <th>Имя региона</th>
        <th>Доставлено в %</th>

    </tr>
    {% for i in 0..data|length-1 %}

    <tr><td>{{ data[i].day|date("d-m-Y") }}</td><td>{{ data[i].regionName }}</td><td>{{ data[i].undelivered ~ '%' }}</td></tr>

    {% endfor %}


</table>

</html>